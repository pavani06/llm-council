# Emenda 2 — Campos aditivos de resiliência do CLI no registro `Run`

**Status: SELADA.** Emenda efetiva desde o ato nomeado do operador registrado
na seção 11 do pré-registro (28/08 16:01).

- **Data:** 28/08/2026
- **Texto-base:** `docs/prereg/2026-08-27-experimento-1vsN.md` na tag `prereg-1vsN`
  (8b82214), com a Emenda 1 (`docs/prereg/2026-08-28-emenda-1.md`, itens 1-12)
  já incorporada.
- **Motivo:** o plano de resiliência do CLI (`docs/plans/2026-08-28-cli-resiliencia.md`,
  aprovado com revisão adversarial Momus OKAY em 28/08) introduz dois campos
  novos no `Run`: `resumed_from` (retomada de parcial por composição, síntese-only,
  acionada por `council ask --resume`) e `stage2_mode` (estágio 2 em modo lite,
  orçável, acionado por `--rank-lite`). O §7 do texto-base congela o schema do
  registro para o experimento 1-vs-N e o piloto ainda não começou; declarar os
  campos nesta emenda, ANTES do piloto, preserva o congelamento sem reabrir
  decisão metodológica selada. Ambos são estritamente aditivos, com leitura
  retroativa definida, e não alteram prompts, roster, perfis, rubricas, manifest,
  endpoint nem regras de engenharia.

## Campos declarados (aditivos ao `Run`)

- `resumed_from`: `string | None` — sha256 do parcial (`<...>-partial.json`) que
  originou o registro por retomada (registro novo referenciando o parcial; o
  parcial nunca é mutado nem removido). `None` ou ausente = execução integral
  (leitura retroativa: todos os registros existentes permanecem válidos).
- `stage2_mode`: `"full" | "lite"` — modo de execução do estágio 2 (avaliadores
  plenos vs. subconjunto orçável, deliberação não plena). Ausente = `"full"`
  (leitura retroativa: registros pré-existentes são de deliberação plena).

## O que esta emenda NÃO muda

- Nenhuma seção metodológica do texto-base (unidades, manifest, rubricas,
  endpoint, cegamento, regra de engenharia) e nenhum item da Emenda 1.
- Nenhum registro existente em `runs/` é reescrito. Continuam valendo os itens 5
  e 6 da Emenda 1: interrupção de infraestrutura permite re-execução; toda
  tentativa iniciada permanece em `runs/`, inclusive parciais; a retomada produz
  registro novo que referencia o parcial por sha e preserva o arquivo original.

---

- **Ato do operador:** 28/08 16:01 — "auorizo".
